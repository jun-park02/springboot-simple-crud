name: springboot-simple-crud
services:
  mysql:
    image: mysql:8.4.8
    ports:
      - "23306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=mydb
      - MYSQL_USER=user
      - MYSQL_PASSWORD=password
    volumes:
      - ./mysql_data:/var/lib/mysql